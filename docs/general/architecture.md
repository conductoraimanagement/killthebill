# KILL THE BILL: High-Level Architecture

## Overview
**KILL THE BILL** is a 3D isometric immersive sim built in **Godot 4** using **GDScript**. The player navigates a world rigidly divided by social class, manipulates a dynamic economy through a "Butterfly Effect" system, and recruits persistent NPCs as agents to topple a corporate oligarchy. All NPC dialogue and world events are generated by an LLM backend.

## Tech Stack
- **Engine:** Godot 4.x
- **Language:** GDScript
- **LLM Integration:** Multi-provider (Gemini, OpenAI, Claude, Qwen, OpenRouter, Local/Ollama)
- **Perspective:** 3D Isometric (Orthogonal Camera3D)
- **Documentation:** HTML (browsable) + Markdown (agent-readable)

## Core Design Pillars
1. **Nature vs. Nurture:** NPCs have immutable intrinsic traits that filter world events into unique behavioral outcomes.
2. **Complex Conjecture:** The LLM evaluates the intersection of ALL world variables to generate emergent events.
3. **Persistent Cast:** ~40 NPCs are generated at playthrough start and persist for the entire run. No spawning/despawning.
4. **Systemic Consequences:** Every player action ripples through the economy, PR system, and social fabric.
5. **Three Victory Paths:** Assassination, Economic Hacking, or Political Revolution — all tracked simultaneously.

## High-Level Flow
1. **Initialization:** `PlayerManager` assigns Class Seed. `RegionGenerator` builds a unique world map (6-10 regions). `WorldDirector` generates 4-6 procedural Oligarchs with unique ambitions. `PopulationDirector` generates the NPC roster.
2. **The Living World:** `WorldDirector` manages the global economy, the NetFeed, and the Oligarch evolution cycle. Each cycle, living Oligarchs take actions based on their ambitions and personality.
3. **NPC Evolution:** `PopulationDirector.evolve_all_npcs()` pushes the world state through each NPC's personality filter.
4. **Player Interaction:** The player builds relationships, recruits agents, and manipulates the simulation.
5. **The Butterfly Effect:** Every action ripples through the economy, PR, and the behavior of both NPCs and Oligarchs.
6. **Victory:** Tracked via three parallel Collapse Meters (Direct Action, Economic Hacking, Political Revolution).

## Directory Structure
```
killthebill/
├── docs/                    # Documentation (HTML + Markdown)
│   ├── index.html           # Dashboard / landing page
│   ├── status.html          # Project timeline & phase tracking
│   ├── styles.css           # Shared CSS for all HTML docs
│   ├── general/             # High-level design docs
│   │   ├── architecture.md  # THIS FILE — system overview
│   │   └── game-design.md   # Setting, narrative, core loop
│   ├── backend/             # Technical backend docs
│   │   ├── world-director.md
│   │   ├── region-generator.md
│   │   ├── llm-manager.md
│   │   ├── population-director.md
│   │   └── netfeed.md
│   ├── frontend/            # Visual and UI docs
│   │   ├── visuals.md
│   │   └── ui-systems.md
│   ├── entities/            # Actor docs
│   │   ├── npc-data.md
│   │   ├── oligarch-data.md
│   │   └── relationships.md
│   ├── systems/             # Gameplay system docs
│   │   ├── heat-evasion.md
│   │   ├── combat.md
│   │   └── victory-conditions.md
│   ├── core/                # HTML: singleton deep-dives
│   ├── entities/            # HTML: entity deep-dives
│   ├── systems/             # HTML: system deep-dives
│   └── phases/              # HTML: roadmap phase pages
├── src/                     # Godot scripts
│   ├── core/                # Singletons (autoloads)
│   │   ├── WorldDirector.gd
│   │   ├── PlayerManager.gd
│   │   ├── PopulationDirector.gd
│   │   ├── LLMManager.gd
│   │   └── CameraController.gd
│   ├── entities/            # Data resources & controllers
│   │   ├── NPCData.gd
│   │   └── PlayerController.gd
│   └── scenes/              # Scene-specific scripts
│       └── GreyboxTestScene.gd
├── project.godot            # Godot project file
└── .gitignore
```

## Singleton Dependency Graph
```
PlayerManager ──────────────────────────────┐
                                            ▼
WorldDirector ◄──── LLMManager         [Game Logic]
     │                  │
     │                  ├── request_npc_action()
     │                  └── request_netfeed_events()
     │
     ├── trigger_news_cycle() ──► LLMManager
     ├── netfeed_event_generated ──► UI
     ├── world_state_changed ──► NPCs, UI
     │
     ▼
PopulationDirector
     ├── generate_new_roster() (once at start)
     └── evolve_all_npcs() (each cycle)
              │
              ▼
         NPCData.process_world_pressure()
              │
              ▼
         get_behavioral_profile() ──► LLM Context
```
